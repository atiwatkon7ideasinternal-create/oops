{
  "name": "oops-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "OOPS! — Organized Online Password Safe (monorepo)",
  "scripts": {
    "dev": "concurrently -n be,fe -c yellow,cyan \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:frontend": "npm --prefix frontend start",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "install:all": "npm --prefix frontend install --legacy-peer-deps && npm --prefix backend install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
