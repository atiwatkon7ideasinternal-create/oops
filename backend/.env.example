# MongoDB connection
MONGO_URI=mongodb+srv://USER:PASS@cluster0.mongodb.net/oops?retryWrites=true&w=majority

# JWT secret for sessions (generate: openssl rand -hex 32)
JWT_SECRET=replace-me-32-bytes-of-random

# CORS origin (frontend dev URL)
CORS_ORIGIN=http://localhost:4321

PORT=3100
